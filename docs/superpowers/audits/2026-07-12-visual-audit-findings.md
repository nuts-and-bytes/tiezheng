# 铁证 IRONPROOF — 阶段 1 人工视觉验收 · 缺陷清单

**日期：** 2026-07-12
**方法：** 本地 `vite preview`（localhost:4173，与线上同一份 `dist/` 产物，origin 隔离，绝不碰线上真实数据）
**覆盖：** 零数据态 / 稀疏态（3 天）/ 丰富态（91 天 + 一个 30 组的怪物日）× 四个 Tab + 月度/年度海报
**运行时验证：** `read_console_messages` 零错误；`read_network_requests` 在海报重绘全程零请求（隐私铁律运行时成立）

---

## A. 真 bug（功能性，必须修）

### B1 · 力量趋势图在单点时全空 —— 主路径 bug
- **现象：** Y 轴刻度与 X 轴日期都画了，图区内一个像素的铁橙都没有（`getImageData` 像素扫描实锤）。
- **触发条件：** 某动作在当前范围内只有 1 个数据点。**App 默认范围是「周」**，所以任何一周内某动作只练过一次的用户（≈ 所有人）打开数据页，默认看到的就是空图。
- **表层根因：** `StatsScreen.tsx:275` `pointRadius: 0`（为求干净线条）→ 单点时既无线段可画、也无点可见；`StatsScreen.tsx:231` 的空态守卫只判断 `!weighted || !active`，没判断 `series.length < 2`。
- **深层根因（第一性原理）：** **信息架构错配**。顶部三个数字（打卡天数/总组数/总容量）是**周期汇总**，天然属于某个时间范围；力量趋势是**进步曲线**，天然属于全时段。把两者绑在同一个「周/月/年/全部」切换器上，必然产生"用户停在默认周 → 趋势图只剩 1 点 → 空白"。
- **修复方向：** 趋势图与周期切换器解耦（始终全时段 / 或最近 N 次）；同时 `pointRadius` 在点少时可见；`series.length < 2` 给降级态（显示该点数值 + "再练一次就有曲线"），而非空图表壳。

### B2 · 海报 footer 文字重叠 —— 成品级缺陷
- **现象：** 白色大字「你练过的，都有铁证。」与灰色小字「TIEZHENG.PAGES.DEV · 本地生成 · 照片不上传」**画在同一条基线上**，糊成一团。
- **影响：** 月度 + 年度**都中招**（共用 footer 绘制代码）。海报是要发出去的成品，糊字直接毁掉整张图。讽刺的是被压糊的正是「本地生成 · 照片不上传」这句隐私承诺。
- **根因：** Canvas 2D 两个 `fillText` 的 y 坐标相同或过近。

### B3 · 年度海报「THE YEAR IN IRON」溢出画布被裁
- **现象：** 大字 `2026` 右侧的英文标签冲出画布右边缘。
- **根因：** 英文标签的起始 x 跟着大字宽度推。`2026`（Anton 压缩体）远宽于月度的 `07`，于是被推出画布。**只在年度海报出现** —— 正是用户点名要的那个。

### B4 · 海报尺寸不固定，且比例非标准
- **实测：** 3 月 `1170 × 2853`（比例 0.410）、7 月 `1170 × 2715`（比例 0.431）。高度随内容（日历行数、训练日多寡）变化。
- **对比：** 标准 9:16 = 0.5625。当前是 ~1:2.4 的极瘦长条。
- **影响：** 每个月导出的海报尺寸都不一样；发朋友圈 / IG story 会被裁剪得乱七八糟。海报的核心用途就是"发出去"。

---

## B. 设计 / 一致性缺陷

| # | 缺陷 | 说明 |
|---|---|---|
| D1 | **零数据"硬摆零"不一致** | 日历顶部 `0 / 0 / 0`、我的页成就墙四个 `0`；而**同一个 app 里**数据页零态、PR 零态都做对了——换成引导文案（「还没有一条铁证。练一次，这里就会长出你的曲线。」）。同根因的一致性缺陷。 |
| D2 | **日历色块过饱和** | p90 归一**本身是对的**（已实测：30 组怪物日没有冲淡其余日子）。但满 alpha 的纯色块（红/蓝/紫）压在深色金属底上像儿童贴纸，与锻造工业风的克制感冲突；白色日期数字压在饱和红/紫上对比度不足。 |
| D3 | **部位均衡语义打架** | 横条统计**范围内**组数（如本周），右侧「已 37 天没练」却是**全时段**事实。同一行里两个时间语义。 |
| D4 | **周环比噪声** | `总容量 4.8t ↓88%` —— 因为上周撞上 30 组怪物日。容量的周环比天然噪声极大，↓88% 不代表用户练差了，但红色下降箭头的心理打击是实打实的。 |
| D5 | **emoji 视觉杂质** | 🔥（连续天数）、📷（拍照占位，甚至渲染成豆腐块方块）、⚒（铁龄）。整套 UI 是线性图标语言，emoji 是破绽。 |
| D6 | **年度热力图缺月份轴标签与图例** | 光秃秃一片色块，用户不知道哪列是几月。日历页有图例，这里没有。 |
| D7 | **日历上月溢出格：有色块但日期暗灰** | 6/29 练了背（蓝色块），日期数字仍是"非本月"的暗灰 —— 有色块却读不出日期。 |
| D8 | **PR 免责声明位置倒置** | 「按预估 1RM（Epley）排名」在列表**最底部**（要滚过 12 条才看到），而虚高的 `252 kg`（腿举 180kg×12 的 Epley 推算）在**最顶部**。免责声明放错了位置。 |
| D9 | **海报双入口文案不一致** | 数据页「生成训练海报」vs 我的页「导出训练海报」，副标题却完全相同。 |
| D10 | **引导页垂直留白过大** | 真机 390×844 下成立（非工装假象），4/4 屏最严重：内容只占顶部 1/4。 |
| D11 | **今日页设计卡组数冗余** | meta 行 `8 组 · 1,160 kg 容量` + 右栏大号 `8组`；无重量数据时 meta 退化成 `3 组`，与右栏完全重复。 |

---

## C. 已验证达成的用户诉求 ✅

- **品牌 logo 出现**：启动屏 / 今日页头 / 我的页 / 数据页零态 均有铁字钢印。
- **各部位有专属图标与颜色**：7 个部位（胸/肩/背/腿/手臂/核心/有氧）在引导、LogFlow 选部位、日历图例、部位均衡中一致使用。
- **日历"一眼看出练了哪个部位"**：色块 + 部位小图标；多部位日显示多图标。
- **p90 归一有效**：30 组怪物日**没有**把 5–8 组的普通日冲淡成灰。若用 `max()` 归一则会。
- **首次引导介绍了海报导出**：第 3 屏明确讲月度/年度海报，并标注「全本地生成 · 照片不上传」。
- **隐私铁律运行时成立**：海报生成全程零网络请求（实测）；「仅存本机」「照片不含在导出文件中」在 UI 可见。
- **数据充足时的可视化是好的**：「全部」范围下 e1RM 曲线（60→90 渐进）、年度热力马赛克、PR 榜 —— 都很扎实。

---

## D. 解决状态（2026-07-13 复核）

清单上的 15 条**全部落地**，逐条给证据；此外在复核中又追出 1 条审计当时没抓到的真缺陷。

### 真 bug

| # | 状态 | 证据 |
|---|---|---|
| B1 | ✅ | `StatsScreen.tsx:245` 趋势区块改吃全时段 `items`，**故意不接范围切换器**（治的是信息架构错配这个根因，而非 `pointRadius`）；`:304` `series.length < 2` 走降级态，`:319`「再练一次，这里就会长出曲线。」；`:342` `pointRadius: 3` |
| B2 | ✅ | `poster.ts:468` `footerLayout(measure, h)` —— footer 改为**量过再排**，两行不再共用基线；`:330` 隐私那句宁可缩字号也不截断 |
| B3 | ✅ | `poster.ts:309` 引入 `Measure` 抽象（绘制时接 `ctx.measureText`，测试里接确定性假字宽），标题小标位置由实测宽度推出，`2026` 不再把英文标签顶出画布 |
| B4 | ✅ | `poster.ts:36/48` `POSTER_W = 390` + `POSTER_SCALE = 2`，`:862` 内容在固定相框内居中 → 导出恒为 1080×1920（9:16），与月份/数据量无关 |

### 设计缺陷

| # | 状态 | 证据 |
|---|---|---|
| D1 | ✅ | `CalendarScreen.tsx:166` 与 `ProfileScreen.tsx:88` 两半都换成引导文案，零数据时**一个 `.display` 数字都不立**（测试锁死） |
| D2 | ✅ | `heat.ts:47` `CALENDAR_ALPHA_CEIL = 0.6` 封顶 + 实色 `HueBar` 把色相钉回去 —— 白色日期数字压得住了 |
| D3 | ✅ | `StatsScreen.tsx:391` 副标题写明两种时间语义：`柱长 = <范围>组数 · 右侧 = 距上次训练（全时段）` |
| D4 | ✅ | `Volume` 组件整个拿掉 delta（容量的周环比噪声远大于信号，这是"不显示"比"显示得更准"更对的一处） |
| D5 | ✅ | 全库 emoji 清零，🔥/📷/⚒ 全换内联 SVG；两条测试用 `\p{Extended_Pictographic}` 守着 |
| D6 | ✅ | `StatsScreen.tsx:480` `heat-months` 月份轴 + `:515` `heat-legend` 部位图例 |
| D7 | ✅ | 溢出格三档浓度（`OVERFLOW_TRAINED = 0.7` / `OVERFLOW_EMPTY = 0.35`）—— 练过的溢出日读得出日期 |
| D8 | ✅ | `ProfileScreen.tsx:109` Epley 说明移到榜首**上方**；测试用 `compareDocumentPosition` 锁住顺序（认知形成前校正） |
| D9 | ✅ | 两处入口统一为「导出训练海报」 |
| D10 | ✅ | `Onboarding.tsx:117` `justify-center-safe`（= `justify-content: safe center`）—— 装得下就居中，装不下退回顶对齐，不再有不可达的顶部 |
| D11 | ✅ | `TodayScreen.tsx:144` meta 行加 `volume > 0` 守卫，组数只在右栏出现一次 |

### 复核中新追出的缺陷（审计当时没抓到）

**D12 · 「我的」页向纯自重训练者摆一个 42px 的 `0 kg`** — 已修（`75c1f7a`）

- **现象：** 只练俯卧撑/引体向上、从不填重量的用户，战绩墙第四格是 `0 kg`，跟「总打卡」并排立着。他读到的不是"我没记重量"，是"我练了等于零"。
- **根因：** 「容量 = 重量 × 次数，对自重用户无意义」这条逻辑在代码库里有**四个**实现点 —— 数据页 (`hasWeightData`)、今日页 (`volume > 0`)、海报 (`formatVolume` → 「—」) 都做了降级，**「我的」页漏了**。
- **修复：** `totals()` 增加 `reps` 维度；第四格按 `hasWeightData(items)` 二分 —— 有重量给「累计容量」，纯自重给「总次数」。**刻意不降级成「—」**：那还是"本该有东西但没有"，而自重训练者的负荷维度本来就是次数。

> **方法论收获（下一轮审查的首要线索）：**
> D12 不是靠看屏幕看出来的，是顺着「同一根因有几个实现点、每个都堵上了吗」追出来的。
> **「同一逻辑在 N 处各写一遍、其中某处漏改」这个模式本身，就是最该被系统性搜捕的缺陷类型。**

---

## 第二轮：对抗式审查（2026-07-13）

第一轮是**看屏幕**看出来的，这一轮是**顺着根因搜**出来的 —— 上一节结尾那条方法论预言的东西，
一条不落地全应验了。13 条真缺陷，每条都先写红测试再修（`npx tsc --noEmit` + 380 tests 全绿）。

### 正确性（用户会拿它当账本，算错就是背叛）

| # | 缺陷 | commit |
|---|---|---|
| A | 日详情里 reps-only 的组**整组消失** —— 写侧存的条件是 `weight OR reps`，读侧渲染的条件是 `weight AND reps`。纯自重用户记了 20 个引体，翻回去看是空的 | `2e110f8` |
| B | JSON 导出**零 `deletedAt` 过滤** —— 软删的记录在导出文件里复活；而且整行 dump，`note`（用户的私人文字）跟着流出去 | `2e110f8` |
| G | 超范围的值被**静默丢弃**（用户以为记上了）；上限还是拍脑袋定的（500kg 误杀腿举机的 560kg） | `0230216` |
| I | Epley 公式没有 reps 上限 —— 60kg×30 算出 e1RM 120kg，把真实的 100kg×1（103.3）顶下了 PR 榜。**公式在高次数区是外推，不是测量** | `04e1e21` |
| J | 环比用「相邻等长窗口」 —— 窗口边界跟着「今天是周几」跑，同一份数据周一看 +18%、周四看 −9%。改成**对齐到周/月边界** | `04e1e21` |
| K | `hasWeightData` 一处传 scoped 一处传全量（口径漂移）；「当前连续」在 4px 内印了两遍；`compare()` 丢掉 reps 维度 | `04e1e21` |

### 设计（同一件事在 N 处各写一遍，其中某处漏改）

| # | 缺陷 | commit |
|---|---|---|
| C | 海报页的月份 Chip 把「7月」套进 Anton —— **Anton 的子集是 95 个 ASCII 字形，零 CJK**。渲染出来是豆腐块 | `b97f3a5` |
| D | `PhotoTimeline` 是全站最后一块实心卡片 —— 用户说的「版块化」本体就是它；缩略图还点不开 | `e1f11f6` |
| E | 年度热力图**纯靠色相**编码部位：9px 见方的格子塞七个色相，红绿色盲（男性约 8%）读到的信息量是零。改成「格子说得出话 + 图例可筛选」，颜色降级为冗余通道 | `17ab99f` |
| F | 「最长连续」三屏三口径（日历页算本月，其余算终身）；「个人纪录」在首页指连续天数、在「我的」页指 e1RM 榜。**一个名词只指一个量** | `fffa846` |
| L | token 在 canvas/SVG 侧被**抄了五份**，且已经抄错：`charts.tsx` 一直用着 iOS 系统灰 `#8e8e93`，而 `--color-mute` 是暖灰 `#8b8b85`。两个灰在暗底上肉眼几乎同色 —— 「暖」这个基调就是从这种地方一点点漏光的 | `004ed03` |
| M | 海报入口的无障碍名被品牌名占位：钢印带着 `aria-label="铁证"` 站在 `<Link to="/poster">` 第一位，读屏用户听到的第一个词是品牌名，而他要判断的是「这链接干什么」 | `440db13` |

（H：`capture="environment"` 写死 → 旧照片无法从相册补录，`0230216`。）

### 两条方法论

**1. 「同一逻辑在 N 处各写一遍」是最高产的搜捕模式。** 上一节写下这句话时它还是个假设；这一轮 A / K / L / M 四条全是它 —— 分别是读写两侧的条件不一致、口径参数传错、色值抄了五份、九处 Stamp 漏了两处。
**修法不是「把这处也改对」，是消灭第 2 个实现点**：L 建了 `src/lib/theme.ts` + 一条逐字比对 `theme.css` 的测试；`dailyPartLoad` 改成 `dailyPartBreakdown` 的派生（决胜规则只写一遍，两个函数不可能各说各话）。
**「只能靠人同步」不是理由，是缺一条测试。**

**2. 没被表决过的存疑项，必须自己补验 —— 不能默认它是假阳性。** M 这条在 Workflow 里是 `0/0`：7 个 refuter agent 全撞上 session limit 挂了，从没有任何人审过它。事后补验，是真的。
**「没人反驳」和「被驳倒了」在计数上都是 0，但在事实上是相反的两件事。**
