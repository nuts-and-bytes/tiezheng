# 复制成 .env.local 再填。不填 = 埋点整个哑掉（不注入脚本、零网络请求），
# 引导页第一屏也会自动把「仅匿名使用统计」那句去掉 —— 每个构建都只承诺它真在做的事。
#
# 这两个值都不是密钥：VITE_ 前缀的变量本来就会被打进客户端 bundle，
# Umami 的 website id 设计上就是公开的。放进仓库唯一的坏处是别人能往你的站点灌脏数据，
# 所以还是走 .env.local。
#
# 这个 .env.local 只管你本机跑 dev / 本地 build 时的埋点。
# 线上构建发生在 CI（.github/workflows/ci.yml 的 deploy job），那边读的是仓库变量：
# 要给线上开埋点就 `gh variable set VITE_UMAMI_WEBSITE_ID --body <uuid>`，不用改一行代码。

# Umami 站点 ID（UUID）。云版：站点 Settings → Tracking code 里那串
VITE_UMAMI_WEBSITE_ID=

# 自托管时填自己的脚本地址；留空走云版 https://cloud.umami.is/script.js
VITE_UMAMI_SRC=
